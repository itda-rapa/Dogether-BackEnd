# 같이놀개 M3 결정사항 및 보완과제

## 1. 확정 반영

| ID | 결정 | 반영 |
|---|---|---|
| A-01 | M3 응답 envelope는 현재 `ApiResponse<T>`를 따른다. | API 전체 |
| A-02 | Chat Media는 Presigned Upload 후 mediaId로 전송한다. | Media·Chat·ERD |
| A-03 | Presigned URL을 메시지 DB·Kafka에 저장하지 않는다. | Chat |
| A-04 | Setlog 공유는 setlogId 참조가 정본이다. | Chat·Setlog |
| A-05 | DIRECT REST/WS와 Open Chat Event의 타입 의미를 일치시킨다. | Chat·Kafka |
| A-06 | RiskSignal은 원문 없이 멱등 event로 저장한다. | Safety |
| A-07 | 자동 위험 제재는 하지 않고 관리자 검토만 수행한다. | Safety·Admin |
| A-08 | AI 장애가 채팅 전달을 막지 않는다. | AI·Open Chat |
| A-09 | Dashboard 기본 7일·최대 90일·KST 경계를 적용한다. | Admin |
| A-10 | 위치 validation과 Meeting 판정을 분리한다. | Location·Meeting |
| A-11 | Footprint는 후기 제출 시 지급하며 동일 상대 하루 1개다. | Review·Footprint |
| A-12 | Redis·ELK·lock 변경은 문제와 측정 지표를 먼저 정의한다. | 보완 작업 |
| A-13 | M3에서는 비로그인 공개를 도입하지 않고 로그인 기반 접근을 유지한다. | Security·API 전체 |
| A-14 | 기존 Board LIKE 필드는 유지하고 HELPFUL 전용 필드를 추가한다. | Board·Front |
| A-15 | 댓글 조회는 Root cursor 기반 nested tree로 변경하고, 대댓글은 별도 endpoint에서 최대 depth 3까지 생성한다. | Board Comment·OpenAPI |
| A-16 | HELPFUL은 Active Pet actor의 Pet+target+type 반응이며, self는 User 기준이다. Post는 LIKE와 HELPFUL, Comment/Reply는 HELPFUL만 지원한다. | Board·Comment·Pet |

## 2. 결정 추적

`D-번호`는 이 문서를 유일한 기준으로 사용한다. 다른 문서에서 같은 번호를 다른 의미로 재사용하지 않는다.

### D-18 HELPFUL 평판 범위 — 확정

- receiver는 Post/Comment 작성 시 저장된 `author_pet_id`다. receiver를 별도 컬럼으로 snapshot하지 않으며 작성자의 이후 Active Pet 변경은 과거 HELPFUL의 귀속을 바꾸지 않는다.
- `PetResponse.helpfulReceivedCount`는 HELPFUL만 합산한다. 삭제 target 자신의 row만 제외하고, 삭제 Post 아래 활성 Comment 및 삭제 parent 아래 활성 descendant는 유지한다. 부모 Post/Comment 상태, Block, reactor Pet의 이후 상태는 Comment aggregate의 제외 조건이 아니다.
- 차단은 신규 반응 접근을 404로 숨기지만 차단 전 row/평판을 제거하지 않는다. Post/Comment unsupported type은 `400 VALIDATION_FAILED`다.
- `GET /pets/{petId}/profile`은 기존 `PetResponse`와 분리된 공개 프로필 endpoint다. HELPFUL 집계 기준은
  그대로 재사용하며 `PetSearchItemResponse`·`PetDisplaySummary`를 확장하지 않는다.
- 10,500+ row·target 5 Pet·background 분산 fixture의 `EXPLAIN ANALYZE`에서 Post/Comment author Pet filter가 각각 약 10,000 rows를 제거했다. undeleted `(author_pet_id, id)` partial index는 Post `3.961ms → 1.789ms`, Comment `1.946ms → 1.341ms`로 scan 범위·buffer·cost를 줄였으므로 V33에 두 index를 추가한다. 이 판단은 이번 fixture 범위에 한정하며 모든 운영 데이터 분포를 보장하지 않는다.

### D-01 비로그인 공개 — 확정

- M3에서는 GUEST 방식을 포함한 비로그인 공개를 도입하지 않는다.
- 현재 로그인 사용자 기반 API와 Security 정책을 유지한다.

### D-02 OAuth 동일 이메일

- 자동 연결
- 기존 계정 본인 확인 후 명시적 연결
- 연결 거부

`users.email`이 대소문자 무시 UNIQUE이므로 별도 User 생성은 선택지에서 제외한다. 권장안은 이메일만으로 자동 연결하지 않고 기존 계정 인증 후 `/auth/oauth/link`로 연결하는 것이다. G1에서 callback→loginCode→exchange, 신규 사용자 `/auth/oauth/signup`, 기존 `AuthTokensResponse` 재사용 범위를 함께 확정한다.

### D-03 Pet 삭제 참조

Open Chat 참여와 과거 메시지·Meeting·Setlog/Post·Media를 삭제할지 표시만 비활성화할지 확정한다. 권장: 과거 기록은 유지하고 신규 행동만 차단한다. Friendship·Pending Request 정리는 회의 확정 요구사항이 아니므로 이 결정에 자동 포함하지 않는다.

### D-04 대댓글 깊이

**확정.** Root `depth=0`, 대댓글 `depth=1~3`을 허용하고 depth 4 생성은 `409 COMMENT_DEPTH_EXCEEDED`로 거절한다. Root 작성(`POST /posts/{postId}/comments`)과 대댓글 작성(`POST /comments/{parentCommentId}/replies`)은 분리하며, 두 요청 body는 strict `content`만 허용한다.

`GET /posts/{postId}/comments`는 flat all-row cursor를 폐기하는 breaking change다. `size`는 Root thread 수이고 cursor는 마지막 반환 Root의 `(createdAt, id)`다. `items`는 `replies`를 포함한 nested tree로 반환한다. Root 및 sibling은 `(createdAt, id)` 오름차순이고, 최종 visible Root candidate에 `size + 1`을 적용해 page를 계산한다.

soft delete는 행을 보존한다. 삭제 노드는 최종 visible active descendant를 잇는 경우에만 nullable tombstone으로 남고, 삭제 leaf는 숨긴다. 양방향 Block 노드는 subtree 전체를 숨기며 unblocked descendant를 승격하지 않는다. 대댓글 작성은 direct parent뿐 아니라 Root→parent 조상 모두의 Block을 검사한다. Parent/Post DELETE와 Reply CREATE 경합은 공유/쓰기 잠금 후 상태 재검증으로, 삭제가 먼저 확정되면 Reply CREATE가 404로 수렴한다.

### D-05 Chat 첨부 제한

결정할 값:

- 메시지당 첨부 개수
- IMAGE/VIDEO MIME·최대 크기
- 영상 길이 3~5초를 누가 검증하는지
- DIRECT/Open Chat 허용 범위

M3 완료선 권장: 메시지당 1개.

### D-06 신고 Media 보존

신고 Evidence인 메시지의 Media를 사용자 삭제 또는 cleanup 시 언제 물리 삭제할지 결정한다. 정책 전에는 참조 해제와 물리 삭제를 분리한다.

### D-07 Setlog 공유 snapshot

- 현재값 참조: 삭제·차단 반영이 쉬움
- 전송 시 snapshot: 과거 맥락 유지, 삭제 후 노출 위험

권장: 현재값 참조, 접근 불가 시 preview 제거.

### D-08 Dashboard 집계

이번 PR의 구현 기준(팀 승인 필요):

- User는 `role=USER`, `accountStatus!=WITHDRAWN`이며 정지 계정은 포함한다.
- Pet은 논리 삭제를 제외하고 정지 Pet은 포함한다.
- Setlog는 `VISIBLE`, `isSeed=false`, BoardPost는 `PUBLISHED`·미삭제만 포함한다.
- Report 생성 수와 RiskSignal 유형·고유 사용자는 요청 기간, Report open·Safety open case·Storage backlog는 조회 시점 현재 상태다.
- RiskSignal 0건 유형은 생략한다.
- 최근 Report·SafetyCase는 요청 기간과 무관한 전체 최신 10건이다.
- Pet도 다른 콘텐츠와 동일하게 `total`, `newInPeriod`를 제공한다.

위 기준은 Dashboard 구현과 검증을 위한 일관된 baseline이다. 각 도메인 담당자 승인 후 D-08을 최종 완료로 전환한다.

### D-09 반복 접촉 임계값

권고 초안: 동일 대상에게 30일 내 거절 후 재접촉 3회 이상이며 최소 2개 날짜. 점수·감쇠·Case unique 범위는 fixture로 검증 후 확정.

### D-10 위치·코드

- 약속 ±1시간
- 제출 간 5분
- 거리 100m
- accuracy 50m 미만
- 코드 4자리, 5분, 5회, 재발급 시 이전 폐기

위 값은 권고 기본값이며 모바일 실측 후 확정한다.

### D-11 위치·산책 보존

정확 좌표, 원시 walk points, 공개 인기 경로의 보존·파기·비식별화 정책을 확정한다.

### D-12 AI 원문·Provider

AI에 보내는 문맥 줄 수, 보존 여부, timeout, 재시도, modelVersion, 사용 고지 문구를 확정한다.

### D-13 Place 식별·표시

- Provider 검색 결과는 `(provider, providerPlaceId)` 기준으로 `places`에 upsert한 뒤 내부 `placeId`를 반환한다.
- 지도 표시 대상과 Place type·Provider별 mapping은 이정수·노동훈이 G1에서 확정한다.

### D-14 Redis 적용 판단

측정 결과와 적용/미적용 근거를 남긴다. 적용한 경우에만 Cache TTL·무효화 또는 Lock 실패 정책과 테스트를 요구한다.

### D-15 ELK·로깅 범위

민감정보 제외 항목, 구조화 로그, retention, 운영 조회 필요성을 먼저 확정한다. 목적 없는 전면 도입은 하지 않는다.

### D-16 산책 경로 완료선

저장·조회 필수 범위와 안내·AI 가중치의 포함 여부, fallback을 이정수·AI 담당이 확정한다.

### D-17 Pet 프로필 이미지 동시성

**확정.** PUT/DELETE 모두 `If-Match`로 현재 Pet `version`을 전달하는 명시적 낙관적
잠금 정책을 사용한다. 값은 큰따옴표로 감싼 0 이상의 10진수 하나(예: `"3"`)이며,
누락·malformed·반복 헤더는 `400 VALIDATION_FAILED`, stale version은
`409 CONCURRENT_UPDATE_CONFLICT`다.

PUT body는 양의 `mediaId`만 허용하고, 요청자 소유의 IMAGE이며 `UPLOADED` 또는
`COMPLETED`인 Media만 연결한다. Media 검증은 같은 Media를 재지정하는 PUT no-op에서도
생략하지 않는다. 실제 link 교체/제거만 Pet `version`을 1 증가시킨다. 같은 Media PUT은
요청 Media를 조회·검증한 뒤 no-op으로 처리하며 version을 유지한다. 이미 비어 있는 DELETE는
별도의 `MediaRepository` command lookup 및 Media 소유권/type/status validation 없이 Pet profile
link 기준 no-op으로 처리하며 version을 유지한다. DELETE는 body·response body·ETag가 없다.

두 API는 Pet의 `profile_asset_id` link만 변경한다. 기존 Media row와 `deletedAt`, S3
객체 및 `StorageDeleteJob` lifecycle은 변경하지 않으며, Media 물리 삭제 정책은 별도
결정사항으로 남긴다.

### D-19 Pet 공개 프로필 가시성 — 확정

- `GET /pets/{petId}/profile`은 인증된 조회자만 사용한다. 대상 Pet은 `ACTIVE`·미삭제이고 소유자 계정도
  `ACTIVE`여야 한다. Pet 부재, 공개 불가 상태, 어느 방향이든 조회자와 소유자 사이의 Block은 모두
  `404 PET_NOT_FOUND`로 existence hiding 한다. 신규 오류 코드는 만들지 않는다.
- 자기 Pet은 조회 가능하지만 `relationship=null`이다. 비자기 대상도 조회자의 Active Pet이 없으면 성공하고
  `relationship=null`이다. Active Pet이 있는 비자기 대상만 기존 FriendRelationship 값을 계산한다.
- 공개 응답은 `PetPublicProfileResponse`의 14개 필드만 사용한다. `ownerUserId`, `ownerPublicTag`,
  `weightKg`, `careNote`, `status`, `deletedAt`, `verifiedAt`, `active`, `version`은 반환하지 않는다.

## 3. 신규 오류 코드

| 영역 | 오류 코드 |
|---|---|
| OAuth | `OAUTH_PROVIDER_UNSUPPORTED`, `OAUTH_LOGIN_CODE_INVALID`, `OAUTH_LOGIN_CODE_EXPIRED`, `OAUTH_LOGIN_CODE_CONSUMED`, `OAUTH_SIGNUP_TOKEN_INVALID`, `OAUTH_SIGNUP_TOKEN_EXPIRED`, `OAUTH_ACCOUNT_LINK_DECISION_REQUIRED`, `OAUTH_PROVIDER_UNAVAILABLE` |
| Pet | `PET_DELETE_REFERENCE_CONFLICT` |
| Board | 기존 `BOARD_POST_COMMENT_NOT_FOUND`, `COMMENT_DEPTH_EXCEEDED`, `PLACE_NOT_FOUND` |
| Media | `MEDIA_METADATA_MISMATCH`, `MEDIA_NOT_READY`, `MEDIA_STORAGE_UNAVAILABLE`, `MEDIA_STORAGE_REJECTED` |
| Chat | `CHAT_MESSAGE_TYPE_INVALID`, `CHAT_MESSAGE_PAYLOAD_INVALID`, `SETLOG_SHARE_FORBIDDEN` |
| Admin | `INVALID_DATE_RANGE`, `DATE_RANGE_TOO_LARGE` |
| Safety | `SAFETY_CASE_NOT_FOUND`, `SAFETY_CASE_ALREADY_CLOSED`, `SAFETY_ACTION_INVALID` |
| Place | `LOCATION_INVALID`, `LOCATION_STALE`, `PLACE_PROVIDER_RATE_LIMITED`, `PLACE_PROVIDER_UNAVAILABLE` |
| Meeting | `MEETING_NOT_PARTICIPANT`, `MEETING_CARD_NOT_OPEN`, `MEETING_TIME_WINDOW_EXCEEDED`, `MEETING_DISTANCE_EXCEEDED`, `MEETING_CODE_MISMATCH`, `MEETING_CODE_EXPIRED`, `MEETING_CODE_ATTEMPTS_EXCEEDED`, `MEETING_ALREADY_CONFIRMED`, `REVIEW_ALREADY_EXISTS` |
| Walk | `WALK_NOT_FOUND`, `WALK_NOT_OWNED`, `WALK_NOT_RECORDING`, `WALK_POINT_INVALID`, `WALK_SEQUENCE_CONFLICT` |

기존 의미가 같은 ErrorCode가 있으면 새 코드를 만들지 않고 기존 코드를 재사용한다.

## 4. 구현 전 체크리스트

- [ ] D-01~D-17 상태와 담당자 기록
- [ ] Controller 경로와 Gateway `/api/v1` 관계 확정
- [ ] Chat REST/WS/Kafka schemaVersion과 serializer 공유
- [ ] Media purpose·MIME·크기·duration 검증
- [ ] Setlog 상세 route와 삭제·차단 UI
- [ ] Dashboard 집계 Query와 KST 경계
- [ ] RiskSignal 멱등키와 점수 fixture
- [ ] Evidence access audit와 보존 정책
- [ ] OAuth Redirect URI·State·PKCE·callback·loginCode/signupToken TTL·동일 이메일 연결 절차
- [ ] Place Provider key·쿼터·약관
- [ ] 위치·AI 사용자 고지와 로그 금지
- [ ] 산책 위치 보존·파기 Job
- [x] D-04 댓글 hierarchy·Root cursor·tombstone·Block subtree 계약 확정

## 5. 테스트 매트릭스

| 영역 | 단위 | 통합 | 계약/E2E |
|---|---|---|---|
| OAuth | provider/code/signupToken 상태 | OAuth callback stub·JWT | 신규 가입·기존 로그인·동일 이메일·code 재사용 |
| Pet | 삭제 상태 | PG 참조 정합성 | Chat/Open Chat 회귀 |
| Board | depth 0~3·strict reply body·HELPFUL | hierarchy FK/CHECK·Root cursor·tombstone·Block subtree·Parent/Post DELETE 경합 | nested tree·OpenAPI·Place·Media Front |
| Media | MIME·상태 | S3/RustFS·cleanup | upload→Chat |
| Chat | 타입별 payload | PG·WS·Kafka | history/realtime 동일 |
| Admin | 기간·집계 | PG Query | Front Dashboard |
| Safety | score·threshold | event inbox·PG | event→Queue→action |
| Place | 좌표·mapping | Provider stub | Board/Meeting/Map |
| Meeting | 거리·시간·코드 | PG 동시성 | 두 계정 E2E |
| Walk | sequence·거리 | batch PG | 시작→points→종료 |
| AI | mapping·timeout | AI stub·Kafka | 채팅 장애 격리 |

## 6. 문서 동기화 규칙

- 필드·상태·오류 변경은 `01`, `03`, `04`, `05`를 함께 갱신한다.
- 구현 PR은 Swagger/OpenAPI와 예제 JSON을 포함한다.
- 업무 범위·목표일 변경은 상위 `M3기준_마일스톤_WBS.md`에 반영한다.
- 팀 결정 후 `결정 대기` 문구와 권고값을 확정값으로 교체한다.
